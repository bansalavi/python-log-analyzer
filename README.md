# python-log-analyzer
A beginner level Python project that reads simulation log files, detects ERROR/WARNING/INFO keywords and generates a summary report.

I have worked on a basic improvement in V2, which saves the summary report in the a text file to analyse later.
Skills Used:
- Python file handling
- Basic automation
- VS Code workflow
